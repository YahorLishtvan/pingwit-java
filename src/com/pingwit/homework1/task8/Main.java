package com.pingwit.homework1.task8;

public class Main { // на будущее называй классы в соответствии с задачей
    public static void main(String[] args) {
        String firstString = "A and B"; // stringResult - у тебя тип переменной String, поэтому из названия его можно убрать firstString -> first или firstWord
        String secondString = " set on the pipe"; // см комментарий выше
        String stringResult = firstString + secondString; // stringResult - у тебя тип переменной String, поэтому stringResult -> result
        System.out.println(stringResult);
    }
}
